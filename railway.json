{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd frontend && npm run build && cd ../backend && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push && node prisma/seed.js && node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
